{
  "mcpServers": {
    "posthog": {
      "command": "npx",
      "args": ["-y", "mcp-remote@latest", "https://mcp.posthog.com/mcp"],
      "env": {
        "POSTHOG_MCP_API_KEY": "${POSTHOG_MCP_API_KEY}"
      }
    }
  }
}